module SurrogateModels

"""
    regression(X,y,m)

Output a regression matrix given inputs

 - `X`: ``n × k`` matrix with `n` training points in `k` dimensional feature space

 - `y`: ``n × 1`` vector of responses (######)

 - `m`: Order of polynomial (Presently only `1` and `2` is supported)

"""
function regression(x,y,m)

    return(x)
end

end
# vim:filetype=julia:foldmethod=syntax


